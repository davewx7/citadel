{
	properties: {
		filter_cost: "def(class player p, class card_base card, int cost) ->int cost",
		filter_base_cost: "def(class player p, class card_base card, int cost) ->int cost",
		filter_loyalty_cost: "def(class player p, class card_base card, int cost) ->int cost",
	}
}
